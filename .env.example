# mock    — дані з локальних фікстур
# hybrid  — реєстр листів з бекенда агента, решта екранів на фікстурах
# http    — все з реального бекенда (запис ще не реалізовано)
VITE_API_MODE=hybrid
VITE_API_BASE_URL=/api/v1
