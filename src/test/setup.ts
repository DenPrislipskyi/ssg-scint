import '@testing-library/jest-dom/vitest';
import { configure } from '@testing-library/react';

/**
 * Скільки чекати на `findBy*` і `waitFor`, поки не здаватися.
 *
 * Типова секунда розрахована на рендер, а тут чекають на ланцюжок: mock-затримка
 * репозиторію, Suspense, дебаунс поля. Під навантаженням — вісімнадцять файлів
 * тестів паралельно — секунди часом не вистачає, і падає той файл, якому не
 * пощастило, а не той, у якому помилка.
 *
 * Це стеля очікування, а не пауза: тест, який проходить, не стає повільнішим
 * ані на мілісекунду.
 */
configure({ asyncUtilTimeout: 3000 });
